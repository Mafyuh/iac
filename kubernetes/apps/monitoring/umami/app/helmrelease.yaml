---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app umami
spec:
  interval: 15m
  chartRef:
    kind: OCIRepository
    name: umami
  values:
    global:
      fullnameOverride: *app

    controllers:
      umami:
        enabled: true
        type: deployment

        replicas: 1

        pod:
          securityContext:
            fsGroup: 1000
            fsGroupChangePolicy: "OnRootMismatch"

        containers:
          app:
            image:
              repository: ghcr.io/umami-software/umami
              tag: 3.3.1@sha256:fa32d116cf20cad52cbc3fad9a63b46e7fa02299d8f967168eb453d49c476b4a
              pullPolicy: IfNotPresent
            env:
              APP_SECRET:
                valueFrom:
                  secretKeyRef:
                    name: umami-secrets
                    key: APP_SECRET
              DATABASE_TYPE: "postgresql"
              DATABASE_URL:
                valueFrom:
                  secretKeyRef:
                    name: umami-secrets
                    key: DATABASE_URL

            probes:
              liveness:
                enabled: false

            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL

            resources:
              requests:
                cpu: 25m
                memory: 512Mi
              limits:
                memory: 1Gi

    service:
      app:
        primary: true
        controller: umami
        ports:
          http:
            port: &port 3000

    route:
      internal:
        enabled: true
        kind: HTTPRoute
        hostnames: ["stats.mafyuh.dev"]
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            backendRefs:
              - name: umami
                port: *port
        parentRefs:
          - name: envoy-external
            namespace: kube-system
            sectionName: https
