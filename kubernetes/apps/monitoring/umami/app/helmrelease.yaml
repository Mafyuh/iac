---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app umami
spec:
  interval: 15m
  chartRef:
    kind: OCIRepository
    name: umami
  values:
    global:
      fullnameOverride: *app

    controllers:
      umami:
        enabled: true
        type: deployment

        replicas: 1

        pod:
          securityContext:
            fsGroup: 1000
            fsGroupChangePolicy: "OnRootMismatch"

        containers:
          app:
            image:
              repository: ghcr.io/umami-software/umami
              tag: 3.3.0@sha256:87312d334d009ee67ee0d2fba8fed01435547cc468e452243aef5133a9984d48
              pullPolicy: IfNotPresent
            env:
              APP_SECRET:
                valueFrom:
                  secretKeyRef:
                    name: umami-secrets
                    key: APP_SECRET
              DATABASE_TYPE: "postgresql"
              DATABASE_URL:
                valueFrom:
                  secretKeyRef:
                    name: umami-secrets
                    key: DATABASE_URL

            probes:
              liveness:
                enabled: false

            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL

            resources:
              requests:
                cpu: 25m
                memory: 512Mi
              limits:
                memory: 1Gi

    service:
      app:
        primary: true
        controller: umami
        ports:
          http:
            port: &port 3000

    route:
      internal:
        enabled: true
        kind: HTTPRoute
        hostnames: ["stats.mafyuh.dev"]
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            backendRefs:
              - name: umami
                port: *port
        parentRefs:
          - name: envoy-external
            namespace: kube-system
            sectionName: https
